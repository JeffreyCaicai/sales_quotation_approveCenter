# Task 1 Report: Standard Node.js Runtime

## Status

DONE_WITH_CONCERNS

## Implementation

- Replaced Vinext/Wrangler scripts with standard `next dev`, `next build`, and `next start` scripts; added the specified Vitest and migration scripts.
- Added `pg`, `zod`, `@types/pg`, `vitest`, and `tsx`; removed the Cloudflare/Vite/Vinext runtime packages named in the brief.
- Enabled Next.js standalone output in `next.config.ts`.
- Replaced the Cloudflare D1 factory with a singleton PostgreSQL `Pool`, `NodePgDatabase<typeof schema>`, required `DATABASE_URL`, and `closeDb()` cleanup.
- Added the runtime contract test and Vitest root alias/configuration, excluding dedicated integration tests.
- Removed the Worker entry point, Sites/Vite integration, hosting metadata, and the two D1-only starter example files.
- Added `allowImportingTsExtensions` for the existing Node strip-types import style and excluded local `exports/` artifacts from TypeScript compilation without reading, modifying, or staging them.
- Included the owner-updated Stage 2 plan file that records the expanded Task 1 file scope.
- Preserved the annotated recovery tag `sites-demo-v1` at `d76497e`; no Sites publish, redeploy, or delete operation was invoked.

## TDD Evidence

### RED

Command:

```sh
npx vitest run tests/runtime-config.test.ts
```

Key output (exit 1):

```text
tests/runtime-config.test.ts (1 test | 1 failed)
AssertionError: expected next.config.ts to contain 'output: "standalone"'
Test Files  1 failed (1)
Tests       1 failed (1)
```

The failure was the intended missing Node standalone runtime contract, not a test syntax or environment error.

### GREEN

Command:

```sh
npx vitest run tests/runtime-config.test.ts
```

Key output (exit 0):

```text
Test Files  1 passed (1)
Tests       1 passed (1)
```

## Complete Verification

Final command (run with controlled network access because `next/font` fetches the existing Google Geist fonts):

```sh
npx vitest run tests/runtime-config.test.ts && npm run test:logic && npm run test:localization && npm run build && test -f .next/standalone/server.js
```

Final result: exit 0.

- Runtime contract: 1/1 passed.
- Quotation logic: 53/53 passed.
- Localization: 20/20 passed.
- Next.js 16.2.6 production build: compiled, TypeScript checked, page data collected, and static pages generated successfully.
- Standalone artifact: `.next/standalone/server.js` exists.

Additional self-check command:

```sh
git diff --check
# dependency/package absence check
# required deletion and standalone artifact existence checks
```

Result: exit 0; no whitespace errors, all seven Cloudflare/Vite/Vinext packages are absent from direct dependencies, all required Cloudflare/D1 files are deleted, and the standalone server artifact exists.

## Build Blockers and Decisions

1. The first sandboxed build failed only because Google Geist font downloads were blocked. It was rerun with controlled network permission as instructed.
2. The network-enabled build then found the D1 example route combining the new PostgreSQL factory with a SQLite example schema. The owner updated the brief and authorized deletion of the two D1-only example files; no PostgreSQL example/schema work from Task 2 was added.
3. The next build type-checked untracked local `exports/` content. The owner authorized excluding `exports` in `tsconfig.json`; the directory was not modified or staged.
4. The next build exposed existing explicit `.ts` production imports. The owner authorized `allowImportingTsExtensions: true`, preserving current Node strip-types tests without expanding business-source changes.

## Changed Files

Modified:

- `package.json`
- `package-lock.json`
- `next.config.ts`
- `tsconfig.json`
- `db/index.ts`
- `docs/superpowers/plans/2026-07-11-stage-2-data-import-vps.md`

Created:

- `tests/runtime-config.test.ts`
- `vitest.config.ts`
- `.superpowers/sdd/task-1-report.md`

Deleted:

- `worker/index.ts`
- `vite.config.ts`
- `build/sites-vite-plugin.ts`
- `.openai/hosting.json`
- `examples/d1/app/api/notes/route.ts`
- `examples/d1/db/schema.ts`

## Self-Review

- Implementation matches the final owner-approved Task 1 brief and does not implement Task 2 schema, migration, authentication, import, or API work.
- `getDb()` uses only `DATABASE_URL`, caps the pool at 10 connections, and exposes the required concrete return type; `closeDb()` clears the singleton after shutdown.
- Standalone output is generated by standard Next.js rather than Vinext/Cloudflare tooling.
- Cloudflare-only source, metadata, scripts, and direct packages are removed.
- The quote Demo's logic and localization suites remain unchanged and fully passing.
- `exports/` remains untracked and unstaged.

## Concerns

- `npm install` reported 14 dependency audit findings (1 low, 7 moderate, 6 high) and install-script approval warnings. These are pre-existing/transitive dependency hygiene concerns and were not changed with `npm audit fix`, which is outside Task 1.
- The production build requires network access when the existing `next/font` cache is cold because Geist and Geist Mono are fetched from Google Fonts.
- The legacy rendered-HTML script was tied to the removed Vinext `test` workflow; the final brief requires the runtime contract, logic, localization, and standard Next build checks, all of which pass.
